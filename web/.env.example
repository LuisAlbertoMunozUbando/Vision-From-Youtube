# Public HTTPS endpoint that Cloudflare Tunnel routes to the Spark worker.
SPARK_API_URL=https://spark-slides.example.com

# Must match SLIDEEXTRACTOR_API_KEY on the Spark. Never prefix with NEXT_PUBLIC_.
SPARK_API_KEY=CHANGE_ME
